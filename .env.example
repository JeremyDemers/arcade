# Shared Google Web Client ID used by both frontends and backends.
GOOGLE_CLIENT_ID='123456789-example.apps.googleusercontent.com'

# Generate a new private value; do not reuse a secret exposed elsewhere.
ARCADE_SECRET='replace-with-a-new-random-value-at-least-32-characters-long'
